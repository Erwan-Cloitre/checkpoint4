{% extends 'base.html.twig' %}

{% block body %}
    <h1>Inscription admin</h1>
    {{ form_start(form) }}
    {{ form_row(form.lastname, {'attr':{'placeholder': 'Adresse email'}}) }}
    {{ form_row(form.firstname, {'attr':{'placeholder': 'Nom'}}) }}
    {{ form_row(form.email, {'attr':{'placeholder': 'Prénom'}}) }}
    {{ form_row(form.password, {'attr':{'placeholder': 'Mot de passe'}}) }}
    {{ form_row(form.roles, {'attr':{'value': '[\'ROLE_ADMIN\']', 'disabled': 'disabled'}}) }}
    <button type="submit" class="btn btn-success">Inscription !</button>
    {{ form_end(form) }}
{% endblock %}